# Specs: FsubL_alt (Active)

- Env model and placeholders will mirror Fsub where applicable (with dual bounds).
- Do not import other Active modules.
- Add only minimal axioms/tactic stubs to Auxiliary.lean to keep typechecking.
