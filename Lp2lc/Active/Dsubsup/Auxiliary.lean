import Std
import Aesop
import «Lp2lc».Shared
import «Lp2lc».Active.Dsubsup.Def

namespace Lp2lc.Active.Dsubsup

-- Minimal placeholder to separate ok (structural) from okt when needed in lemmas
axiom ok : env -> Prop

end Lp2lc.Active.Dsubsup
