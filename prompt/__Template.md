can you convert all coq files under `Lp2lc_coq/Active` according to tasks in `prompt/Setup.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Setup/*.md`

---

can you convert all coq files under `Lp2lc_coq/Active` according to tasks in `prompt/Report.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Report/*.md`

---

can you convert all coq files under `Lp2lc_coq/Active` according to tasks in `prompt/Scaffold.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Scaffold/*.md`

---

can you convert all coq files under `Lp2lc_coq/Active` into Lean according to tasks in `prompt/Discharge.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Discharge/*.md`