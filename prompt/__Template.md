
can you convert all coq files under `Lp2lc_coq/Active` according to task defined in `prompt/Define.md`? Write/update your plans into `gen/plan/Define/*.md` and code into `Lp2lc/Active`

---

can you convert all coq files under `Lp2lc_coq/Active` into Lean according to task defined in `prompt/Discharge.md`? Write/update your plans into `gen/plan/Discharge/*.md` and code into `Lp2lc/Active`