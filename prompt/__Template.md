
can you convert all coq files under `Lp2lc_coq/Active` according to task defined in `prompt/Define.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Define/*.md`

---

can you convert all coq files under `Lp2lc_coq/Active` into Lean according to task defined in `prompt/Discharge.md`? Write/update code into `Lp2lc/Active` and plans into `gen/plan/Define/*.md`